{% extends '@Frontend/layout.html.twig' %}

{% block opengraph %}
    <meta property="og:type" content="article" />
    <meta property="og:title" content="Blog | Compuglobal" />
    <meta property="og:description" content="Enterate de todo lo que pasa en el mundo de la tecnología. Compuglobal a
    la vanguardia de la información que necesitas." />
    <meta property="og:image" content="{{ absolute_url(asset('uploads/images/'~img.imagen)) }}" />
{% endblock %}

{% block publicaciones %}
    class="active"
{% endblock %}

{% block title %}{{ parent() }} | Publicaciones{% endblock %}

{% block body %}
<div class="row no-margin">
    <div class="col-md-12">
        <div class="container blog-page">
            <h2 class="text-center">Artículos</h2>
            {% for item in articulos %}
                <div class="col-md-4 col-sm-6">
                    <div class="blog-item shadow-dark">
                        <a href="{{ path('article',{'slug':item.slug}) }}">
                            <img class="img-responsive blog-img" src="{{ asset('uploads/images/'~item.imagen) }}">
                        </a>
                        <a href="{{ path('article',{'slug':item.slug}) }}">
                            <h3>{{ item.titulo }}</h3>
                        </a>
                        <p>{{ item.descripcion }}</p>
                        <p>Publicado: {{ item.updatedAt|localizeddate('long','none','es',null,"eee d 'de' LLL yyyy") }}
                            <a href="{{ path('article',{'slug':item.slug}) }}"><img src="{{ asset('assets/img/plus.png') }}"></a>
                        </p>
                    </div>
                </div>
            {% endfor %}
            <div class="row margin-tb">
                <div class="col-xs-12"><img class="img-responsive" src="{{ asset('uploads/images/'~img.imagen) }}"></div>
            </div>
        </div>
    </div>
</div>
{% endblock %}